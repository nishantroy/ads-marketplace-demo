# Neon Postgres connection string (used from M3 onward). Keep the real value in .env.local, which is git-ignored.
DATABASE_URL=postgres://user:password@ep-example.neon.tech/ads_marketplace?sslmode=require
